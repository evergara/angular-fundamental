import { UrlImagenPipe } from './url-imagen.pipe';

describe('UrlImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlImagenPipe();
    expect(pipe).toBeTruthy();
  });
});

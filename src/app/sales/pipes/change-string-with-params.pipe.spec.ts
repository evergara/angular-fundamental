import { ChangeStringWithParamsPipe } from './change-string-with-params.pipe';

describe('ChangeStringWithParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeStringWithParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
